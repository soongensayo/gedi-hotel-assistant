# Passport scanner pipeline (core/scanner.py)
#
# EasyOCR pulls in opencv-python-headless as a transitive dep, which has no
# cv2.imshow and will crash the camera preview. After installing, always run:
#
#   pip install --force-reinstall opencv-python
#
# This replaces the headless build with the full GUI version.
opencv-python>=4.5.0
numpy>=1.19.0
Pillow>=9.0.0
easyocr>=1.6.0
python-dotenv>=1.0.0
